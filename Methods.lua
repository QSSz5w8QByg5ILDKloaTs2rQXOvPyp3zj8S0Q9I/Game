return {
    Ffc = game.FindFirstChild,
    Ffcoc = game.FindFirstChildOfClass,

    FromRGB = Color3.fromRGB,

    Vector3New = Vector3.new,
    Vector3Zero = Vector3.zero,
    Vector2New = Vector2.new,

    CFrameNew = CFrame.new,
    CFrameAngles = CFrame.Angles,

    MathRandom = math.random,
    MathFloor = math.floor,
    MathMassive = math.huge, -- massive? you know what else is massive?
    MathClamp = math.clamp,
}
